#include <stdio.h>
int main() {
    int a=4,b=6,lcm;
    lcm=(a>b)?a:b;
    while(1) {
        if(lcm%a==0 && lcm%b==0)
            break;
        lcm++;
    }
    printf("LCM = %d", lcm);
    return 0;
}
